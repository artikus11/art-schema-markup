language: php

sudo: false

php:
- '5.6'
- '7.0'
- '7.1'
- '7.2'

script:
# Run your tests here.
- echo 'Tested'

before_deploy:
      # Set up git user name and tag this commit
      - git config --local user.name "artikus11"
      - git config --local user.email "artikus.sol@gmail.ru"
      - git tag "$(date +'%Y%m%d%H%M%S')-$(git log --format=%h -1)"

deploy:
  provider: releases
  api_key:
    secure: "b27945544c0122ba5e8d6ace0e0beda30ed90397"
  file: "latest.zip"
  skip_cleanup: true
  on:
      tags: true

after_deploy: "release created"


